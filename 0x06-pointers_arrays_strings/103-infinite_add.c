#include "main.h"
#include <string.h>

/**
 * infinite_add - function that adds two numbers
 * @n1: function parameter
 * @n2: function parameter
 * @r: function parameter
 * @size_r: function parameter
 * Return: 0(Success)
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{
	int i;

	for (i = 0 ; n1[i] != '\0', i++)
